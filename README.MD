### Task #1 – Use `PreparedStatement`

Take the project from the previous homework.  
In this project, replace all `Statement` usages with `PreparedStatement` for any SQL queries that accept parameters.